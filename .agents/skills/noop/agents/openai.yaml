interface:
  display_name: "Noop"
  short_description: "Repo-specific patterns and workflows for noop"
  default_prompt: "Use the noop repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true